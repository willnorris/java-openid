<?xml version="1.0" encoding="UTF-8"?>
    <!-- Basic configuration used until the system can load the real configuration -->
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</Pattern>
        </layout>
    </appender>

    <logger name="edu.internet2.middleware.openid">
        <level value="DEBUG" />
    </logger>
    
    <logger name="edu.internet2.middleware.openid.message">
        <level value="INFO" />
    </logger>

    <root>
        <appender-ref ref="STDOUT" />
        <level value="WARN" />
    </root>

</configuration>